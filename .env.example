# GitHub API token for accessing repositories
# Required for integration tests that query GitHub API
# Generate at: https://github.com/settings/tokens
GITHUB_TOKEN=your_github_token_here

# Optional: Enable slow integration tests (package installation tests)
# Uncomment to run these tests (they can take several minutes)
# RUN_INTEGRATION_TESTS=1
